/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["https://my-website-git-master-pilishhs-projects.vercel.app/"], // Agrega el dominio si es necesario
  },
};

export default nextConfig;
